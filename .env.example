# Optional. If left empty, TGTLDR will generate a random key on first start
# and persist it to /var/lib/tgtldr/master.key inside the app volume.
# Set it explicitly only if you want to manage the key yourself.
TGTLDR_MASTER_KEY=
