# Changelog

[English version](CHANGELOG.en.md)

## 2026-09-06

### 修复

- 每日总览重新生成后自动刷新摘要列表的归入状态，关闭总览侧栏后仍会同步至生成结束，无需手动刷新页面。

### 调整

- 摘要保存请求模型与上游返回模型；两者不同时显示“请求模型（实际返回：返回模型）”，历史记录保留原模型标签。
- 模型返回空白正文时按临时失败进行有限重试，避免误标成功；总览对空正文给出具体遗漏原因。

- 每日总览默认仅显示“重新生成并发送”，仅在生成成功但发送失败时提供“重试发送”。

- 每日总览支持按最新群组参与范围“重新生成并发送”；来源未就绪时提示具体群组，保留原总览。
- 当日总览已创建但未包含的群组显示“未纳入当日总览”，避免无限显示等待。

- 单群摘要与每日总览采用最多四次阶梯重试，依次等待 1、3、5、10 分钟；升级时已开启的自动重试调整为四次，保留关闭状态。
- 群组推送选项改为“纳入每日总览”，移除撑高字段的说明文案。
- Telegram 每日总览不再显示网页来源编号，网页仍保留引用。

- 启用每日总览时自动让所有已开启 AI 总结的群组参与推送，包括原先仅网页查看的群组；启用说明同步明确参与范围，后续仍可单独取消群组参与。

## 2026-09-05

### 修复

- 修复从其他页面进入摘要页时，每日总览说明文案会因设置异步加载而闪动的问题；加载期间不再预设未启用状态。

## 2026-09-04

### 新增

- 新增“每日总览”推送方式：等待所有参与 Bot 推送的群组完成单群每日摘要后，自动归并为一篇跨群总览并发送到 Telegram。
- 摘要页新增每日总览历史与详情侧栏，可查看来源摘要、遗漏或无消息群组，并手动重新生成或重试发送。

### 调整

- 群组的 Telegram 推送设置现在负责决定是否参与推送；系统设置可选择按群分别发送或合并为每日总览，模式变更从保存当天的消息开始生效。
- 每日总览与快速回顾共用多来源摘要的上下文预算、自动分块和多层归并能力。
- 摘要页在每日总览未启用时改为显示启用入口，并通过确认弹窗说明参与范围、生效日期以及是否同时开启 Telegram Bot。
- README 现在在同一个文件中提供完整中文和英文说明，不再单独维护英文 README。

## 2026-08-23

### 新增

- 摘要页新增“快速回顾”：可选择最近 7、14、30 天或最多 90 天的自定义日期范围，并从启用 AI 摘要的群组中生成一份可追溯来源的阶段回顾。
- 快速回顾支持后台生成、历史记录与详情侧栏；已配置 Telegram Bot 时默认同时发送结果，并可单独重试失败的投递。

### 调整

- 每日摘要和快速回顾现在会根据模型上下文长度与实际 token 数优先使用完整输入；只有容量不足或上游实际报告上下文超限时才分块，并支持多层合并兜底。
- 摘要引擎设置新增上下文长度的自动识别与手动配置，便于为自定义模型和 OpenAI-compatible 服务提供准确容量。

## 2026-07-21

### 新增

- 群组列表新增全局聊天记录搜索，可跨群组检索消息、发言人和用户名；点击结果可进入对应群组当天的聊天记录并定位到消息上下文。
- 群组聊天记录支持自动下载和显示 WebP 静态贴纸、WebM 视频贴纸与 TGS 动画贴纸；升级后启动账号监听时会自动补建历史贴纸资源。

### 调整

- 无文字的图片、文件和贴纸不再向摘要 transcript 或聊天记录正文注入“无文字说明”占位文本。

### 修复

- 修复实时监听会忽略当前 Telegram 账号自己发送的群消息的问题。
- 修复匿名管理员或以群组、频道身份发送的消息可能显示为 `Unknown` 的问题；升级后会在后台分批修复已有历史消息，并在修复完成前使用群组名称兜底显示。

## 2026-07-17

### 修复

- 修复 iOS Safari 底部悬浮地址栏区域不再显示网页内容的问题，同时保留手机端底部导航的安全间距与阴影。
- 修复手机端群组消息活跃度和账号删除提示撑宽页面、导致页面可向右拖出空白区域的问题。
- 修复登录页画布未延伸至 iOS Safari 底部悬浮地址栏后方的问题。
- 修复仪表盘透明外层在 iOS Safari 底部悬浮地址栏后方停止绘制的问题。

### 调整

- 新增独立的 Web 监听地址配置，支持向局域网开放网页端，同时保持后端 API 仅本机可访问。
- 摘要页顶部统计卡可直接清空现有条件并切换到全部、成功、处理中或失败摘要筛选。

## 2026-07-16

### 新增

- 新增群组聊天记录：支持按日期浏览和加载更早消息、群内搜索、按群组设置过滤消息，并可直接查看当天摘要；同时优化手机端浏览体验。
- 群组聊天记录现支持自动下载并内联查看图片、视频、音频、语音和普通文件，同时展示已获取的发言人及群组头像；超过 100 MB 的文件可手动确认下载，失败任务可重试。

### 调整

- 群组列表将群名与公开用户名统一排列在群图标右侧；没有可用头像或头像加载失败时显示默认群组图标，避免列表错位。
- 系统偏好设置新增“自动下载消息附件”开关；关闭后，新附件和尚未开始的附件会等待用户手动下载，头像下载不受影响。

## 2026-07-14

### 调整

- 摘要的原始 Prompt 现在通过独立侧栏展示；关闭后会回到原摘要详情，桌面端和手机端均可直接查看和滚动 Prompt 内容。
- 摘要详情和原始 Prompt 增加进出场动画：桌面端从右侧滑入，手机端从底部进入；打开 Prompt 时继续复用原有蒙版，不再叠加模糊效果。

### 修复

- 修复从摘要侧栏查看原始 Prompt 时，Prompt 窗口可能出现在蒙版后方、需要滚动页面才能看到的问题。
- 修复 0 条消息等空上下文摘要打开原始 Prompt 时可能触发客户端异常的问题。

## 2026-07-13

### 调整

- 网页端现已适配手机浏览：仪表盘使用底部导航，群组列表会转换为便于触控的卡片，摘要筛选和详情阅读也针对窄屏重新排版。

### 修复

- 修复桌面端摘要详情侧拉可能被长内容撑宽、超出预期宽度的问题。

## 2026-07-12

### 新增

- 支持在同一个 TGTLDR 实例中绑定和管理多个 Telegram 账号；群组仍只保留一条记录，并可选择由哪个账号接收消息和加载历史消息。

### 调整

- 系统配置页重新组织为多个分类标签，并优化多账号管理、Bot 目标会话绑定和页面切换体验；群组列表会展示多账号群组当前使用的账号。

### 修复

- 修复添加 Telegram 账号时，发送验证码后登录卡片可能意外收起的问题。

## 2026-07-11

### 新增

- 群组列表中的群组名称现在可直接跳转到摘要页，并自动筛选该群组的摘要。

## 2026-06-14

### 新增

- 群组列表现在会在每个群组下方展示最近 30 天每日消息活动条，并可通过自定义悬停提示快速查看当天消息数。
- 摘要列表现在会在每条摘要的日期和模型旁显示生成摘要时使用的消息数。

### 修复

- Telegram 用户会话遇到 `AUTH_KEY_DUPLICATED` 等永久认证错误时，现在会自动清空失效 session 并标记为未连接，避免监听和历史回补继续复用坏 session。

## 2026-06-06

### 调整

- Docker Compose 默认会在 Docker daemon 或宿主机重启后自动恢复 `postgres`、`app` 和 `web` 服务。

## 2026-05-10

### 新增

- OpenAI 配置现在支持连接测试，便于在初始化向导或系统设置中先验证接口是否可用。
- 支持了流式请求，以应对中转站对非流式请求不友好、可能导致超时报错的问题。
- 新增摘要失败自动重试策略，可配置重试次数、起始间隔和退避倍率。
- 失败摘要详情页现在支持查看并复制 OpenAI 请求参数、System prompt 和 User prompt，方便排查模型调用问题。

### 调整

- 失败摘要详情页会显示自动重试状态和下一次重试时间，便于判断任务后续处理情况。
- 优化 Docker 镜像构建流程，减少不必要的重建并提升发布速度。
- 新增按日期维护的中英文 changelog，并在 README 中补充入口链接。

### 修复

- 失败详情页的复制按钮增加了回退实现，在不支持 `navigator.clipboard` 的环境下也尽量支持复制，并在失败时给出明确提示。

## 2026-05-08

### 新增

- 支持为 Telegram 用户客户端配置 `socks5://` 或 `socks5h://` 代理，便于在受限网络环境下完成登录、同步群组和监听消息。

### 调整

- 补充 Telegram 代理配置说明以及 Docker 环境下的使用提示，降低自部署时的配置成本。
